/**
* Interface defining the component's options object
*/
export interface Loopback4TenantTableFilterComponentOptions {
  // Add the definitions here

}

/**
* Default options for the component
*/
export const DEFAULT_LOOPBACK4_TENANT_TABLE_FILTER_OPTIONS: Loopback4TenantTableFilterComponentOptions = {
  // Specify the values here

};
